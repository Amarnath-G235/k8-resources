apiVersion: v1
kind: pod
metadata:
  name: nginx
    #labels:
    #namespace: expense
    #if we don't mention namespace, it will go to default namespace
spec:
  containers: 
  - name: nginx
    image: nginx
  # ports: # This is optional
  # - containerPort: 80

