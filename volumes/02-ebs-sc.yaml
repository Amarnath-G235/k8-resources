apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: expense-ebs
provisioner: ebs.csi.aws.com
volumeBindingMode: WaitForFirstConsumer # storage will be created at the time of pod creation